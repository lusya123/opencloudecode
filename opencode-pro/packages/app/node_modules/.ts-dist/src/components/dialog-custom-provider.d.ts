import { Component } from "solid-js";
interface ModelEntry {
    id: string;
    name: string;
}
export declare const DialogCustomProvider: Component<{
    providerId?: string;
    initialData?: {
        name: string;
        baseUrl: string;
        apiKey: string;
        models: ModelEntry[];
    };
}>;
export {};
