export type KeybindConfig = string;
export interface Keybind {
    key: string;
    ctrl: boolean;
    meta: boolean;
    shift: boolean;
    alt: boolean;
}
export interface CommandOption {
    id: string;
    title: string;
    description?: string;
    category?: string;
    keybind?: KeybindConfig;
    slash?: string;
    suggested?: boolean;
    disabled?: boolean;
    onSelect?: (source?: "palette" | "keybind" | "slash") => void;
    onHighlight?: () => (() => void) | void;
}
export declare function parseKeybind(config: string): Keybind[];
export declare function matchKeybind(keybinds: Keybind[], event: KeyboardEvent): boolean;
export declare function formatKeybind(config: string): string;
export declare const useCommand: () => {
    register(cb: () => CommandOption[]): void;
    trigger(id: string, source?: "keybind" | "palette" | "slash" | undefined): void;
    keybind(id: string): string;
    show: () => void;
    keybinds(enabled: boolean): void;
    suspended: () => boolean;
    readonly options: CommandOption[];
}, CommandProvider: (props: import("solid-js").ParentProps<Record<string, any>>) => import("solid-js").JSX.Element;
