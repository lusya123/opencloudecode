import { EventSessionError } from "@opencode-ai/sdk/v2";
type NotificationBase = {
    directory?: string;
    session?: string;
    metadata?: any;
    time: number;
    viewed: boolean;
};
type TurnCompleteNotification = NotificationBase & {
    type: "turn-complete";
};
type ErrorNotification = NotificationBase & {
    type: "error";
    error: EventSessionError["properties"]["error"];
};
export type Notification = TurnCompleteNotification | ErrorNotification;
export declare const useNotification: () => {
    ready: import("solid-js").Accessor<boolean>;
    session: {
        all(session: string): Notification[];
        unseen(session: string): Notification[];
        markViewed(session: string): void;
    };
    project: {
        all(directory: string): Notification[];
        unseen(directory: string): Notification[];
        markViewed(directory: string): void;
    };
}, NotificationProvider: (props: import("solid-js").ParentProps<Record<string, any>>) => import("solid-js").JSX.Element;
export {};
