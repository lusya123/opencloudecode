export type SessionScroll = {
    x: number;
    y: number;
};
type ScrollMap = Record<string, SessionScroll>;
type Options = {
    debounceMs?: number;
    getSnapshot: (sessionKey: string) => ScrollMap | undefined;
    onFlush: (sessionKey: string, scroll: ScrollMap) => void;
};
export declare function createScrollPersistence(opts: Options): {
    cache: Record<string, ScrollMap>;
    drop: (keys: string[]) => void;
    flush: (sessionKey: string) => void;
    flushAll: () => void;
    scroll: (sessionKey: string, tab: string) => SessionScroll;
    seed: (sessionKey: string) => void;
    setScroll: (sessionKey: string, tab: string, pos: SessionScroll) => void;
    dispose: () => void;
};
export {};
