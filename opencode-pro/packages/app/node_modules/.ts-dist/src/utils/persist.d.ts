import { type Accessor } from "solid-js";
import type { SetStoreFunction, Store } from "solid-js/store";
type InitType = Promise<string> | string | null;
type PersistedWithReady<T> = [Store<T>, SetStoreFunction<T>, InitType, Accessor<boolean>];
type PersistTarget = {
    storage?: string;
    key: string;
    legacy?: string[];
    migrate?: (value: unknown) => unknown;
};
export declare const Persist: {
    global(key: string, legacy?: string[] | undefined): PersistTarget;
    workspace(dir: string, key: string, legacy?: string[] | undefined): PersistTarget;
    session(dir: string, session: string, key: string, legacy?: string[] | undefined): PersistTarget;
    scoped(dir: string, session: string | undefined, key: string, legacy?: string[] | undefined): PersistTarget;
};
export declare function removePersisted(target: {
    storage?: string;
    key: string;
}): void | Promise<void>;
export declare function persisted<T>(target: string | PersistTarget, store: [Store<T>, SetStoreFunction<T>]): PersistedWithReady<T>;
export {};
