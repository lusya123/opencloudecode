import z from "zod";
declare const prefixes: {
    readonly session: "ses";
    readonly message: "msg";
    readonly permission: "per";
    readonly user: "usr";
    readonly part: "prt";
    readonly pty: "pty";
};
type Prefix = keyof typeof prefixes;
export declare namespace Identifier {
    function schema(prefix: Prefix): z.ZodString;
    function ascending(prefix: Prefix, given?: string): string;
    function descending(prefix: Prefix, given?: string): string;
}
export {};
