import {
  access,
  asArray,
  tryOnCleanup
} from "./chunk-EPEP5WPS.js";
import {
  isServer
} from "./chunk-5C2DUIQJ.js";
import {
  createEffect,
  createRenderEffect
} from "./chunk-VL2SL2GY.js";

// ../../node_modules/.bun/@solid-primitives+event-listener@2.4.3+95b571dd5236cc92/node_modules/@solid-primitives/event-listener/dist/eventListener.js
function makeEventListener(target, type, handler, options) {
  target.addEventListener(type, handler, options);
  return tryOnCleanup(target.removeEventListener.bind(target, type, handler, options));
}
function createEventListener(targets, type, handler, options) {
  if (isServer)
    return;
  const attachListeners = () => {
    asArray(access(targets)).forEach((el) => {
      if (el)
        asArray(access(type)).forEach((type2) => makeEventListener(el, type2, handler, options));
    });
  };
  if (typeof targets === "function")
    createEffect(attachListeners);
  else
    createRenderEffect(attachListeners);
}

export {
  makeEventListener,
  createEventListener
};
//# sourceMappingURL=chunk-WLAEE5HL.js.map
